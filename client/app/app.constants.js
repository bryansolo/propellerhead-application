(function() {
    "use strict";   
    /* TODO: add a build system like Gulp and inject environment variables like this that vary from beta to gamma to prod */
    angular.module("propellerhead").constant("API_SERVER", "http://localhost:3000");
})();